namespace PawnShop.Models
{
    public enum CurrencyType
    {
        Undefined = 0,   // undefined currency type
        Money,           // cash currency
        MetalParts,      // scraps, tools, gears
        Plastics,        // cases, parts
        FiberFabric,     // canvas, leather, textiles
        WoodRemnants,    // frames, handles, old items
        Electronics,      // boards, wires, chips
        GlassCeramics    // lenses, dishes, tiles, decorative elements
    }
}